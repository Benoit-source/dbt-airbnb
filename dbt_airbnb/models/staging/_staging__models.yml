version: 2

models:
  - name: stg_airbnb__listing
    description: '{{ doc("stg_airbnb__listing") }}'

    columns:
      - name: ville
        description: '{{ doc("stg_airbnb__listing_id") }}'
      - name: id
        tests:
          - not_null

  - name: stg_airbnb__review
    description: '{{ doc("stg_airbnb__review") }}'

    columns:
    - name: listing_id
      tests:
        - not_null
